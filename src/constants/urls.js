const baseURL = "https://jsonplaceholder.typicode.com";

const posts = "/posts"
const users = "/users"
const comments = "/comments"

const urls = {
    posts,
    users,
    comments
}

export {
    baseURL,
    urls
}